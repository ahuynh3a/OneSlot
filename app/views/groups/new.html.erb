<div class="container">
   <div class="row justify-content-center align-items-center">
      <div class="col-md-6">
         <div class="d-flex justify-content-between align-items-center" style="margin-bottom: 20px;">
            <h1 style="font-size: 24px; flex-grow: 1;">New Group</h1>
            <%= link_to "Back to groups", user_groups_path(username: current_user.username), class: 'btn btn-secondary' %>
         </div>
         <hr>
         <%= render "form", group: @group %>
         <br>
      </div>
   </div>
</div>
