<p style="color: green"><%= notice %></p>

<%= render @event %>

<div>
  <%= link_to "Edit this event", edit_event_path(@event) %> |
  <%= link_to "Back to events", user_events_path(username: current_user.username) %>

  <%= button_to "Destroy this event", @event, method: :delete %>
</div>
